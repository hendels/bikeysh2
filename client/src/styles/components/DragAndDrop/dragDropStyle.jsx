
import styled from 'styled-components';

export const Container = styled.div`
    display:flex;
    flex-direction: row;
`;
const dragDropStyle = () => ({
    spinner: {
        textAlign: "center",
    }
});

export default dragDropStyle;